"""Edge Card static-site generator.

Pure-Python (Streamlit-free) rendering of the per-game "Edge Card" research
sheets and the surrounding site, built from ``data/output/latest.json``. The
decision/read layer is reused verbatim from ``app/ui.py`` and ``project547``;
only the presentation is new.
"""
