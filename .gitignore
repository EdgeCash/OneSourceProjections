# secrets
.env
.streamlit/secrets.toml

# data caches (regenerated by the pipeline)
data/cache/
*.parquet

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# editors
.vscode/
.idea/
.DS_Store

# nflverse PBP — free, re-downloadable; not committed (see data/history/pbp/nfl/README.md)
data/history/pbp/**/*.parquet

# Local agent scratch tooling (smoke harness, backtest sweeps) — not part of the repo
scratchpad/
